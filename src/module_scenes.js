/*
 *  Containing:
 *    - Slowly increasing strife during duels. 
 *    - Allowing user to 'bid' strife with a popup that reveals itself at the end of the round. 
 *    - Also allows user to automatically update initiative after the round is added, strife also added. 
 */
// JavaScript source code
export function init() {

}